using Auction.DataAccess.Postgres.Entities;

namespace Auction.App.Models;

public class BidModel
{
    /*private BidModel(Guid id, Guid lotId, Guid userId, decimal bid, DateTime timeStamp)
    {
        Id = id;
        LotId = lotId;
        UserId = userId;
        Bid = bid;
        TimeStamp = timeStamp;
    }
    public Guid Id { get; set; }
    public Guid LotId { get; set; }
    public Guid UserId { get; set; }
    public decimal Bid {  get; set; }
    public DateTime TimeStamp { get; set; }

    public (BidModel Bid, string Error) Create(Guid id, Guid lotId, Guid userId, decimal bid, DateTime timeStamp)
    {
        var error = string.Empty;
        
        
        var Bid = new BidModel(id, lotId, userId, bid, timeStamp);
    }*/
}